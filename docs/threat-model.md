# Threat Model

Assets include data, credentials, model inputs and outputs, provenance, and availability. Threats include malicious inputs, secret leakage, unauthorized access, dependency compromise, poisoning, and denial of service. Controls: validation, least privilege, isolation, secret hygiene, audit evidence, dependency auditing, and deterministic tests.
